# 毕业设计论文审查报告

## 一、参考文献问题 ❌

### 问题描述
1. **摘要第一段就出现[18]**，不符合学术规范（应该从[1]开始）
2. **参考文献顺序混乱**：正文中出现顺序为 [18] → [2] → [6][9] → [5] → [7][10] → [3][4]...
3. **缺少上标格式**：参考文献标注应为上标+粗体

### 需要调整
重新排序参考文献，按正文中首次出现的顺序编号：
- 第一次出现：摘要中的"关键[18]" → 改为 [1]
- 第二次出现：摘要中的"WebRTC 推流[2]" → 改为 [2]
- 依此类推...

## 二、表格分隔符问题 ❌

### 问题描述
表格内容中也包含空格，导致"文本转表格"功能会错误切割

### 解决方案
将表格列分隔符从"空格"改为"---"（三个连字符）

示例：
```
表4-1  数据库主要表结构
表名---作用---关键字段---说明
users---用户表---user_id、username、password_hash、role、created_at---保存登录用户、密码哈希和角色信息
```

## 三、论文内容审查

#### ⚠️ 存在的问题

### 3.2 可能被老师质疑的点

#### 问题1：硬件系统描述不足 ⚠️
**位置：** 全文
**问题：** 课题要求"硬件系统包括摄像头，处理器，通信模块等"，但论文中：
- 没有专门章节描述硬件选型
- 没有说明处理器型号（树莓派？PC？）
- 没有说明通信模块（WiFi？4G？）
- 没有硬件框图

**建议：**
- 在第3章或第4章增加"3.X 硬件系统设计"小节
- 说明：摄像头型号、处理器平台、无线通信方式
- 或在现有章节中补充硬件说明

树莓派是5，这个我之后给你相关具体型号，你先随便写一些型号。然后注意在后面的测试中也表明测试用的型号

#### 问题2：AI接口预留不明确 ⚠️
**位置：** 全文
**问题：** 课题要求"留出后续可以采用AI辅助故障识别的接口"，但论文中：
- 没有提到AI接口设计
- 没有说明如何扩展AI功能
- 数据库设计中没有相关字段

**建议：**
- 在第4章"数据库设计"中增加：
  - `ai_detection_results` 表（预留）
  - `device_snapshots` 表（保存关键帧用于AI分析）
- 在第7章"展望"中明确说明AI扩展方案

在第7章"展望"中明确说明AI扩展方案，不改代码了



**4.3 "实时动态码率调整"表述不准确**
- 论文说"实时动态码率调整"
- 实际是：重建推流连接，切换预设档位
- 不是真正的"实时"调整（需要重连）

**建议：** 改为"基于心跳检测的分档降级机制"

### 3.3 叙述与代码不闭环的地方

#### 问题1：心跳间隔不一致
**论文：** "每隔固定时间间隔"（未明确）
**代码：** `HEARTBEAT_INTERVAL = 10` 秒
**建议：** 论文中明确写"每隔10秒"

#### 问题2：超时阈值不一致
**论文：** "超过超时阈值"（未明确）
**代码：** `timeout_seconds = 30`
**建议：** 论文中明确写"30秒超时阈值"

#### 问题3：降级触发条件不精确
**论文：** "连续多次心跳失败"
**代码：**
```python
if failures >= 3: return "poor"
if failures >= 1: return "weak"
```
**建议：** 论文中明确写"1次失败切换weak，3次失败切换poor"

#### 问题4：数据库引擎不一致
**论文：** 多处提到"数据库"，但未说明类型
**代码：** 使用 SQLite
**建议：** 论文中明确说明"采用 SQLite 轻量级数据库"

### 3.4 格式和表述优化建议

#### 优化1：标题与内容的呼应
**问题：** 标题强调"端到端"和"无线"，但正文中：
- "端到端"概念不够突出
- "无线"传输的特点描述不足

**建议：**
- 在摘要和绪论中强调"端到端"：从摄像头到显示端的完整链路
- 增加无线网络特点的描述：WiFi环境、信号衰减、移动场景

#### 优化2：WebRTC 核心地位不够突出
**问题：** 标题是"基于 WebRTC"，但：
- 第2章技术介绍中 WebRTC 篇幅较少
- 没有专门说明为什么选择 WebRTC

**建议：**
- 在2.2节增加"WebRTC 在巡检场景中的优势"
- 对比 RTSP/RTMP/HLS，说明 WebRTC 的低延迟优势

#### 优化3：测试数据需要更具体
**问题：** 第6章测试结果：
- "约 0.5-1.0 秒" - 太模糊
- "画面清晰" - 主观描述
- "较低" - 不够量化

**建议：**
- 给出具体测试数据：平均时延 0.8s，最大 1.2s
- 用客观指标：分辨率、帧率、码率
- CPU占用：具体百分比

这个数据你自己编

### 3.5 缺失的关键内容

#### 缺失1：系统部署说明
- 没有说明如何部署到实际环境
- 没有说明硬件要求
- 没有说明网络要求

**建议：** 在附录中增加部署说明，可以参考命令.txt

#### 缺失2：安全性考虑
- 没有提到视频流的安全性
- 没有提到认证机制的安全性
- MediaMTX 匿名访问的风险

**建议：** 在第7章"不足"中提到安全性问题

这个先不管，后面我改代码

