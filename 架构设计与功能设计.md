# 基于 WebRTC 的电厂巡检机器人端到端无线视频传输系统
## 架构设计与功能设计说明文档

---

## 一、课题目标与约束

### 1.1 课题目标
设计并实现一种端到端的无线视频传输系统，用于电厂巡检机器人远程监控场景。系统一端为充当机器人视觉的摄像头，另一端为远程巡检人员的显示端。通过机器人巡检，将实时画面传输到人工端进行故障识别，并在系统中留出后续可以采用AI辅助故障识别的接口。

### 1.2 技术约束与要求
- **硬件系统**：包括摄像头、处理器（树莓派5）、通信模块（WiFi 5）
- **软件系统**：适合使用场景的视频传输协议（WebRTC）
- **开发语言**：Python
- **视频质量**：不小于720P分辨率
- **实时性**：端到端延迟不高于1秒
- **自适应能力**：带宽受限环境下可自适应调节码率，减少丢帧和卡顿
- **扩展性**：预留AI辅助故障识别接口

---

## 二、总体架构设计

### 2.1 系统架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        电厂巡检机器人视频传输系统                  │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────┐         ┌──────────────────┐         ┌──────────────────┐
│  摄像头采集端     │         │   流媒体服务      │         │   Web监控端       │
│  (camera-client) │ ◄─────► │   (MediaMTX)     │ ◄─────► │  (web-client)    │
│                  │  WHIP   │                  │  WHEP   │                  │
│  - 树莓派5       │  推流   │  - 流接入转发     │  拉流   │  - Vue3前端      │
│  - USB摄像头     │         │  - 协议转换       │         │  - WebRTC播放    │
│  - Python采集    │         │  - 多路分发       │         │  - 设备管理UI    │
│  - WebRTC推流    │         │                  │         │                  │
└──────────────────┘         └──────────────────┘         └──────────────────┘
         │                                                          │
         │                                                          │
         │                  ┌──────────────────┐                   │
         └─────────────────►│   后端服务        │◄──────────────────┘
                   HTTP API │   (FastAPI)      │    HTTP API
                            │                  │
                            │  - 用户认证       │
                            │  - 设备管理       │
                            │  - 心跳检测       │
                            │  - 状态上报       │
                            │  - SQLite数据库   │
                            └──────────────────┘
```

### 2.2 架构特点

1. **前后端分离**：业务逻辑与媒体传输分离，后端不转发视频数据
2. **端到端传输**：摄像头 → MediaMTX → 浏览器，完整的视频链路
3. **无线通信**：WiFi 5（802.11ac）支持，适应移动场景
4. **低延迟设计**：WebRTC协议，端到端延迟<1秒
5. **模块化设计**：各组件职责清晰，便于独立部署和维护

---

## 三、硬件系统设计

### 3.1 摄像头采集端硬件配置

| 组件 | 型号/规格 | 说明 |
|------|----------|------|
| 处理器 | 树莓派5（Raspberry Pi 5） | 64位四核Cortex-A76，主频2.4GHz |
| 内存 | 4GB LPDDR4X | 满足视频编码和推流需求 |
| 摄像头 | USB摄像头 | 支持720P及以上分辨率 |
| 无线模块 | WiFi 5（802.11ac） | 2.4GHz/5GHz双频，理论带宽433Mbps |
| 存储 | MicroSD卡 | 存储操作系统和程序代码 |
| 电源 | 5V/3A USB-C | 为树莓派供电 |

### 3.2 服务端硬件配置

| 组件 | 配置 | 说明 |
|------|------|------|
| 服务器 | Windows PC或Linux服务器 | 运行后端、MediaMTX |
| 网络 | 千兆以太网 | 局域网内稳定连接 |
| 存储 | 普通硬盘 | 存储数据库文件 |

### 3.3 客户端硬件配置

| 组件 | 要求 | 说明 |
|------|------|------|
| 设备 | PC、笔记本或平板 | 支持现代浏览器 |
| 浏览器 | Chrome 90+或Edge 90+ | 支持WebRTC |
| 网络 | WiFi或有线网络 | 局域网内访问 |

---

## 四、软件系统设计

### 4.1 技术栈选型

#### 4.1.1 摄像头采集端（camera-client）
- **语言**：Python 3.9+
- **视频采集**：OpenCV (cv2.VideoCapture)
- **WebRTC推流**：aiortc
- **HTTP客户端**：aiohttp
- **配置管理**：python-dotenv

#### 4.1.2 后端服务（server）
- **框架**：FastAPI
- **数据库ORM**：SQLAlchemy
- **数据库**：SQLite
- **认证**：JWT + bcrypt
- **异步支持**：asyncio

#### 4.1.3 流媒体服务（MediaMTX）
- **软件**：MediaMTX
- **协议支持**：WHIP、WHEP、RTSP、HLS
- **端口**：8554(RTSP)、8889(WebRTC)、8888(HLS)

#### 4.1.4 前端客户端（web-client）
- **框架**：Vue 3
- **构建工具**：Vite
- **语言**：TypeScript
- **状态管理**：Pinia
- **UI组件库**：Element Plus
- **路由**：Vue Router
- **HTTP客户端**：axios

### 4.2 为什么选择WebRTC？

| 协议 | 延迟 | 浏览器支持 | 适用场景 | 本系统是否采用 |
|------|------|-----------|---------|--------------|
| RTSP | 1-3秒 | 需插件 | 传统监控 | 仅用于推流备选 |
| RTMP | 2-5秒 | 需插件 | 直播推流 | 不采用 |
| HLS | 5-15秒 | 原生支持 | 点播/直播 | 不采用 |
| WebRTC | 0.5-1秒 | 原生支持 | 实时通信 | ✓ 主要方案 |

**选择WebRTC的理由**：
1. 端到端延迟低（<1秒），满足实时监控需求
2. 浏览器原生支持，无需插件
3. 内置拥塞控制和自适应码率机制
4. 支持P2P传输，减轻服务器压力
5. WHIP/WHEP标准化接入，降低开发复杂度

---

## 五、核心功能设计

### 5.1 设备注册与管理

#### 5.1.1 设备注册流程
```
采集端启动 → 读取配置 → 向后端POST /api/devices/register
→ 后端生成device_id → 返回device_id → 采集端保存device_id
→ 使用device_id作为MediaMTX流路径
```

#### 5.1.2 设备信息管理
- **设备表字段**：device_id、device_name、device_type、location、ip_address、status
- **状态管理**：online、offline
- **操作**：查询、删除、更新

### 5.2 心跳检测机制

#### 5.2.1 心跳参数
- **心跳间隔**：10秒
- **超时阈值**：30秒
- **监控任务间隔**：6秒

#### 5.2.2 心跳流程
```
采集端每10秒 → POST /api/devices/{device_id}/heartbeat
→ 后端更新last_heartbeat → 设置status=online
→ 后端监控任务每6秒检查 → 超过30秒未心跳 → 设置status=offline
```

### 5.3 视频传输链路

#### 5.3.1 推流链路（WHIP）
```
摄像头采集 → OpenCV读取帧 → aiortc编码
→ WHIP协议推流 → MediaMTX接收
→ 流路径：/{device_id}/whip
```

#### 5.3.2 拉流链路（WHEP）
```
前端请求播放 → 构造WHEP地址：/{device_id}/whep
→ WebRTC连接MediaMTX → 接收视频流
→ HTMLVideoElement播放
```

### 5.4 弱网自适应降级机制

#### 5.4.1 三档参数配置

| 档位 | 分辨率 | 帧率 | 码率 | 触发条件 |
|------|--------|------|------|---------|
| good | 1280x720 | 25fps | 2000kbps | 心跳连续成功 |
| weak | 960x540 | 20fps | 1200kbps | 连续失败1次 |
| poor | 640x360 | 15fps | 600kbps | 连续失败3次 |

#### 5.4.2 降级触发逻辑
```python
if consecutive_failures >= 3:
    network_level = "poor"
elif consecutive_failures >= 1:
    network_level = "weak"
else:
    network_level = "good"
```

#### 5.4.3 档位切换实现
- 重建WHIP推流连接
- 应用新的编码参数（分辨率、帧率、码率）
- 不是真正的"实时动态"调整，而是分档切换

### 5.5 用户认证与权限

#### 5.5.1 认证流程
```
用户登录 → POST /api/auth/login → 验证用户名密码
→ 生成JWT token → 返回token → 前端保存token
→ 后续请求携带token → 后端验证token
```

#### 5.5.2 权限设计
- **当前实现**：role字段预留，未实现细粒度权限
- **后续扩展**：admin、operator、viewer角色区分

### 5.6 系统安全性设计

#### 5.6.1 用户认证安全
- **JWT认证**：用户登录后生成JWT令牌，有效期24小时
- **密码哈希**：使用bcrypt算法哈希存储密码，避免明文存储
- **默认配置检测**：启动时检测默认密钥，输出安全警告

#### 5.6.2 设备接口认证
- **设备API令牌**：设备注册、心跳、状态上报需要携带 X-Device-Token
- **令牌验证**：后端验证令牌是否正确，防止未授权设备接入
- **配置方式**：通过环境变量 DEVICE_API_TOKEN 配置

#### 5.6.3 CORS跨域控制
- **开发环境**：可配置 CORS_ORIGINS="*" 允许所有来源
- **生产环境**：明确指定允许的前端地址，如 "http://192.168.1.100:5173"
- **安全提示**：启动时检测CORS配置，输出警告信息

#### 5.6.4 流媒体安全
- **关闭录像**：MediaMTX配置中关闭录像功能（record: no）
- **网络隔离**：系统部署在局域网内，通过网络隔离保证安全
- **生产建议**：启用MediaMTX的publish/read认证、启用HTTPS

#### 5.6.5 安全措施总结
| 安全措施 | 实现方式 | 说明 |
|---------|---------|------|
| 用户认证 | JWT + bcrypt | 登录认证和密码哈希 |
| 设备认证 | X-Device-Token | 防止未授权设备接入 |
| CORS限制 | 白名单配置 | 限制前端访问来源 |
| 密钥检测 | 启动时警告 | 提示修改默认配置 |
| 关闭录像 | MediaMTX配置 | 不实现未使用功能 |

#### 5.6.6 生产环境安全建议
1. 启用HTTPS加密传输
2. 配置MediaMTX流媒体鉴权
3. 定期更新密钥和令牌
4. 限制数据库访问权限
5. 配置防火墙规则
6. 定期审计系统日志

---

## 六、数据库设计

### 6.1 用户表（users）

| 字段 | 类型 | 说明 |
|------|------|------|
| user_id | String(36) | 主键，UUID |
| username | String(50) | 用户名，唯一 |
| password_hash | String(255) | bcrypt哈希密码 |
| role | String(20) | 角色（预留） |
| created_at | DateTime | 创建时间 |
| updated_at | DateTime | 更新时间 |

### 6.2 设备表（devices）

| 字段 | 类型 | 说明 |
|------|------|------|
| device_id | String(36) | 主键，UUID |
| device_name | String(100) | 设备名称 |
| device_type | String(50) | 设备类型 |
| location | String(200) | 设备位置 |
| ip_address | String(50) | IP地址 |
| status | String(20) | 在线状态 |
| stream_url | String(500) | 流地址 |
| resolution | String(20) | 分辨率 |
| fps | Integer | 帧率 |
| bitrate | Integer | 码率 |
| network_level | String(20) | 网络等级 |
| stream_status | String(50) | 流状态 |
| reconnect_count | Integer | 重连次数 |
| last_heartbeat | DateTime | 最后心跳时间 |
| created_at | DateTime | 创建时间 |
| updated_at | DateTime | 更新时间 |

---

## 七、API接口设计

### 7.1 认证接口

| 接口 | 方法 | 功能 |
|------|------|------|
| /api/auth/login | POST | 用户登录 |
| /api/auth/me | GET | 获取当前用户信息 |
| /api/auth/logout | POST | 用户登出 |
| /api/auth/change-password | POST | 修改密码 |

### 7.2 设备管理接口

| 接口 | 方法 | 功能 |
|------|------|------|
| /api/devices/register | POST | 设备注册 |
| /api/devices/{device_id}/heartbeat | POST | 设备心跳 |
| /api/devices/{device_id}/stream-status | POST | 视频状态上报 |
| /api/devices | GET | 设备列表 |
| /api/devices/{device_id} | GET | 设备详情 |
| /api/devices/{device_id}/offline | POST | 设备下线 |
| /api/devices/{device_id} | DELETE | 删除设备 |

---

## 八、AI辅助故障识别接口预留

### 8.1 预留设计思路

虽然当前系统未实现AI功能，但在架构设计中预留了扩展接口：

#### 8.1.1 数据层预留
- **设备快照表（device_snapshots）**：定期保存关键帧
  - snapshot_id、device_id、image_path、timestamp、resolution
- **AI检测结果表（ai_detection_results）**：存储AI分析结果
  - result_id、snapshot_id、detection_type、confidence、bbox、timestamp

#### 8.1.2 接口层预留
- **POST /api/ai/analyze**：提交图像进行AI分析
- **GET /api/ai/results/{device_id}**：查询设备的AI检测历史
- **POST /api/ai/alerts**：AI异常告警接口

#### 8.1.3 前端预留
- **告警展示模块**：显示AI检测到的异常
- **历史记录页面**：查看AI分析历史

### 8.2 AI扩展实施路径

1. **第一阶段**：关键帧定期保存（每5秒保存一帧）
2. **第二阶段**：集成AI模型（YOLO、ResNet等）
3. **第三阶段**：实时检测与告警（异常自动通知）
4. **第四阶段**：历史数据分析与趋势预测

---

## 九、系统测试指标

### 9.1 功能测试
- 用户登录/登出
- 设备注册/删除
- 心跳检测
- 实时视频播放
- 弱网自动降级/恢复

### 9.2 性能测试
- **端到端延迟**：正常网络<1秒，弱网<2秒
- **视频质量**：720P@25fps（good档位）
- **CPU占用**：树莓派5采集端35-45%
- **内存占用**：采集端200-300MB

### 9.3 弱网测试
- 带宽限制测试
- 丢包率测试
- 网络抖动测试
- 降级/恢复测试

---

## 十、部署架构

### 10.1 开发环境部署
- Windows PC：运行后端、前端、MediaMTX
- 树莓派5：运行采集端
- 局域网：WiFi连接

### 10.2 生产环境部署（建议）
- 云服务器：部署后端和MediaMTX
- 树莓派5：部署在巡检机器人上
- 4G/5G网络：支持移动场景

---

## 十一、风险与答辩准备

### 11.1 可能被问到的问题

1. **为什么选择WebRTC而不是RTSP？**
   - 答：WebRTC延迟更低（<1秒），浏览器原生支持，适合实时监控

2. **弱网降级是如何实现的？**
   - 答：基于心跳失败次数判断网络质量，重建推流连接应用新参数

3. **AI接口如何预留？**
   - 答：数据库预留快照表和结果表，API预留分析接口，前端预留告警模块

4. **系统安全性如何保证？**
   - 答：JWT认证、密码哈希、HTTPS（生产环境）、流媒体访问控制

5. **如何保证720P和<1秒延迟？**
   - 答：WebRTC协议、zerolatency编码、MediaMTX直接转发、测试验证

### 11.2 系统亮点

1. 端到端完整实现（摄像头→浏览器）
2. 基于WebRTC的低延迟传输
3. 弱网自适应降级机制
4. 前后端分离架构
5. 预留AI扩展接口

---

## 十二、总结

本系统设计并实现了一套完整的端到端无线视频传输系统，满足电厂巡检机器人远程监控需求。系统采用WebRTC协议实现低延迟传输，通过弱网自适应机制提高稳定性，并预留AI辅助故障识别接口。架构清晰、模块化设计、便于扩展和维护。
